{% extends "publico_base.njk" %}
{% block titulo %}Publico | Producto{% endblock %}
{%block estilos%}
    <link rel="stylesheet" href="/css/estilos.css"/>{%endblock%}
    {% block contenido %}
        <div class="row mt-4">

            <img class="col-3" src="/uploads/{{producto.imagen}}" alt="{{producto.nombre}}"/>
            <div class="col-9">

                <p>
                    <h3>{{producto.nombre}}</h3>
                    <p>
                        <p>
                            {{producto.precio}}
                            €</p>
                        <p>{{producto.descripcion}}</p>
                    </div>
                </div>

            </div>

        </div>
        <div class="row ml-4 mt-4">
            <h3>Comentarios</h3>
        </div>

        <div class="row">
            <img src="" alt="" class="col-2">
            <div class="col-10">
                <p class="mb-3">
                    <h2>{{user.login}}</h2>
                </p>
                <p>{{usuario.comentario}}</p>
            </div>

        {% endblock %}