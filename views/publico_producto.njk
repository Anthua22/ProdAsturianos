{% extends "publico_base.njk" %}
{% block titulo %}Publico | Producto{% endblock %}
{%block estilos%}
    <link rel="stylesheet" href="/css/estilos.css"/>{%endblock%}
    {% block contenido %}
        <div class="row mt-4">

            <img class="col-3" src="/uploads/{{producto.imagen}}" alt="{{producto.nombre}}"/>
            <div class="col-9">
                <p> <h3>{{producto.nombre}}</h3><p>
                <p>{{producto.precio}} €</p>
                <p>{{producto.descripcion}}</p>
            </div>
                

            

        </div>
        <div class="row ml-4 mt-4">
            <h3>Comentarios</h3>
        </div>

        {% for comentario in producto.comentarios %}
        <div class="ml-4 mt-4 caja_comentario">

            <p class="mb-3">
                <h3>{{comentario.nombre}}</h3>
            </p>
            <p class="oblique">{{comentario.comentario}}</p>
        </div>
        {%endfor%}


        {% endblock %}